spring.application.name=luberight-remote-api
server.port=8082

spring.config.import=optional:file:./.env[.properties]

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.open-in-view=false

management.endpoints.web.exposure.include=health,info

# Two-phase sync (remote-api -> backend)
backend.api.base-url=${BACKEND_API_BASE_URL:http://localhost:8081}
sync.interval=${SYNC_INTERVAL:7200000}
sync.initial-delay=${SYNC_INITIAL_DELAY:7200000}
